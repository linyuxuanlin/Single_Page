# v0.6 训练轨迹复盘数据层

本轮在 `session.mjs` 中新增了可直接供网页复盘可视化使用的两个接口。

## `extractTrainingEvents(session)`

从一次训练样本中定位具体关键时刻，而不是只返回聚合统计：

- `maxLateral`：最大横向偏差点，保留正负方向。
- `maxHeading`：最大航向误差点，保留正负角度。
- `maxSpeed`：最高速度点，`value` 为 km/h。
- `firstLineTouch`：首次进入触线状态的样本；连续压线不会重复生成首次事件。
- `steeringChanges`：方向由左变右或由右变左的关键修正节点。
- `gearChanges`：D/R 挡切换节点。
- `completion`：首次达到完整入库判定的样本。

每个事件都带 `index`、`t`、后轴坐标、航向、速度、转角和挡位，可直接用来在 Three.js 场景中放置标记或把回放时间轴跳到对应时刻。

## `buildReplayTrajectory(session, { maxPoints })`

将约 10 Hz 的完整训练样本压缩成适合手机端绘制的轨迹数据。算法遵循两个原则：

1. 首点和末点必须保留。
2. 最大偏差、首次触线、最高速度、完成入库、方向切换和换挡等关键样本优先保留。

因此后续即使把数百个训练样本压缩到约 120–180 个绘制点，也不会为了降低 GPU/内存开销而丢失最值得复盘的时刻。

## 下一步 UI 接入

网页复盘层可以直接基于这两个接口实现：

- 用户轨迹与标准轨迹同时叠加；
- 最大横向偏差点使用醒目标记；
- 首次触线点显示红色事件标记；
- 方向切换/换挡节点显示小型时间轴事件；
- 点击复盘指标时，将场景视角定位到对应关键姿态。

## 验证

`session-tests.mjs` 已新增关键事件与轨迹压缩回归测试，覆盖：关键事件索引、偏差符号、速度单位、首次触线、换挡节点、首尾保留、关键点在降采样后仍存在，以及空会话/零点限制安全处理。

本地执行：

```bash
node session-tests.mjs
```

当前结果：`session-tests: all assertions passed`。
